import { Skeleton } from "@/components/ui/skeleton";
import React from "react";

const HeroSectionLoading = () => {
  return <Skeleton className="w-full max-h-96 aspect-video p-2 flex" />;
};

export default HeroSectionLoading;
